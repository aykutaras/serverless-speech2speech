export const main = async (event, context, callback) => {
    // check transcribe job with speechId
    // download transcript from s3 ????
    // Update speech data with sourceSpeechText and speechId
};
